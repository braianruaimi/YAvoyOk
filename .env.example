# ====================================
# YAVOY v3.1 ENTERPRISE - CONFIGURACIÓN DE EJEMPLO
# ====================================
# ⚠️ Copia este archivo a .env y completa con tus propios valores

# 📊 BASE DE DATOS POSTGRESQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=yavoy_db
DB_USER=postgres
DB_PASSWORD=tu_contraseña_aqui
DB_POOL_MIN=2
DB_POOL_MAX=20
DATABASE_URL=postgresql://postgres:tu_contraseña_aqui@localhost:5432/yavoy_db

# 🔐 SEGURIDAD ENTERPRISE
NODE_ENV=development
PORT=5502
WS_PORT=5501

# JWT Secret - Genera uno único: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=genera_tu_propio_jwt_secret_de_al_menos_64_caracteres_aleatorios
JWT_EXPIRES_IN=24h

# Session Secret - Genera uno único similar al JWT
SESSION_SECRET=genera_tu_propio_session_secret_de_al_menos_64_caracteres_aleatorios

# Seguridad CEO Enterprise
CSRF_SECRET=genera_tu_csrf_secret
ENCRYPT_SECRET=genera_tu_encryption_key
CEO_RATE_LIMIT_MAX=10
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=1800000

# 💳 MERCADOPAGO
# Para obtener credenciales: https://www.mercadopago.com.ar/developers/panel
# Usa credenciales de TEST para desarrollo, PRODUCTION para producción
MERCADOPAGO_ACCESS_TOKEN=TEST-YOUR-ACCESS-TOKEN-HERE
MERCADOPAGO_PUBLIC_KEY=TEST-YOUR-PUBLIC-KEY-HERE
MERCADOPAGO_WEBHOOK_SECRET=tu_webhook_secret_aqui

# 🌐 CORS - Orígenes permitidos (separados por coma)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5502,http://localhost:5503,http://127.0.0.1:3000,http://127.0.0.1:5502,http://127.0.0.1:5503,https://tudominio.com

# 📧 EMAIL (SMTP Configuration)
# Para Gmail: https://support.google.com/mail/answer/185833
# Para Hostinger: Panel de control > Email
SMTP_HOST=smtp.tu-proveedor.com
SMTP_PORT=587
SMTP_USER=tu-email@dominio.com
SMTP_PASS=tu_contraseña_de_aplicacion
SMTP_SECURE=false
SMTP_TLS=true

# 🔔 NOTIFICACIONES PUSH (VAPID Keys)
# Genera tus propias keys: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=tu_vapid_public_key_aqui
VAPID_PRIVATE_KEY=tu_vapid_private_key_aqui
VAPID_SUBJECT=mailto:tu-email@dominio.com

# ========================================
# 🚀 CONFIGURACIÓN ENTERPRISE ADICIONAL
# ========================================

# 📡 WEBSOCKETS GPS OPTIMIZADO
WEBSOCKET_HEARTBEAT=30000
GPS_UPDATE_INTERVAL=5000
PING_TIMEOUT=60000
PING_INTERVAL=25000
CONNECTION_RECOVERY_TIMEOUT=120000

# 🌐 CONFIGURACIÓN DOMINIO
FRONTEND_URL=http://localhost:5502
API_URL=http://localhost:5502/api
WEBSOCKET_URL=http://localhost:5501

# 📝 LOGGING ENTERPRISE
LOG_LEVEL=debug
LOG_FILE=logs/yavoy.log
ERROR_LOG_FILE=logs/error.log
ACCESS_LOG_FILE=logs/access.log

# 🛡️ RATE LIMITING AVANZADO
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100
WEBHOOK_RATE_LIMIT=50

# 🔧 SFTP HOSTINGER (Para deploy en producción)
SFTP_HOST=tu-servidor.hostinger.com
SFTP_PORT=22
SFTP_USERNAME=tu-usuario-hostinger
SFTP_PASSWORD=tu-contraseña-hostinger
SFTP_REMOTE_PATH=/public_html/yavoy/

# 🎨 CONFIGURACIÓN UI PREMIUM
THEME_MODE=premium
ANIMATION_SPEED=300
TOAST_DURATION=5000
PREMIUM_ANIMATIONS=true
GLASS_EFFECTS=true

# 🚨 SEGURIDAD CEO AVANZADA
CEO_SESSION_TIMEOUT=1800000
CEO_REQUIRE_2FA=true
CEO_IP_WHITELIST=
CEO_GEOLOCATION_CHECK=true

# ====================================
# 📝 NOTAS IMPORTANTES
# ====================================
# 1. NUNCA subas el archivo .env real a GitHub
# 2. Genera secrets únicos para cada ambiente (dev/prod)
# 3. Usa credenciales de TEST para desarrollo
# 4. Configura backup automático de la base de datos
# 5. Rota los secrets regularmente en producción
# 6. Para generar secrets seguros usa: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
